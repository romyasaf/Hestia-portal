# Next.js + pnpm: hoist React/Next so the dev server can always resolve `react-dom`
# (avoids intermittent "Can't resolve 'react-dom'" / blank 500 error pages).
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
public-hoist-pattern[]=*next*
