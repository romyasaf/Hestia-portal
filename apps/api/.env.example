# Required — API will not start without these (see ENVIRONMENT.md)
DATABASE_URL=postgresql://app_user:app_password@localhost:5432/property_ops
JWT_ACCESS_SECRET=generate_with_openssl_rand_base64_48
JWT_REFRESH_SECRET=generate_a_different_secret_with_openssl_rand_base64_48

# Optional
PORT=4000
