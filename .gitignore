node_modules
.next
.expo
dist
build
.DS_Store
.env
.env.local
coverage
*.log
# Local Next.js uploads (listing images); keep .gitkeep only
apps/web/public/uploads/**
!apps/web/public/uploads/.gitkeep
# Workspace uses a single lockfile at repo root; ignore stray per-package lockfiles.
apps/*/pnpm-lock.yaml
packages/*/pnpm-lock.yaml
yarn.lock
package-lock.json
